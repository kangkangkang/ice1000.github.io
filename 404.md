---
layout: base
title: 时间旅行发生错误，你在冰封之地失去了方向
---

```ruby
class ErrorReasoner
	def initialize
		@message = '你来到这个页面，通常有两个原因'
	end
	def _print_reason
		print @message, "\n"
	end
end

printer = ErrorReasoner.new
printer._print_reason
```

```java
public class ErrorReasoner {
	public ErrorReasoner() {
	}
	public final String message = "一、链接错误";
	public void printMessage() {
		System.out.println(message);
	}
	public static void main(String[] args) {
		ErrorReasoner reasoner = new ErrorReasoner();
		reasoner.printMessage();
	}
}
```

```kotlin

class ErrorReasoner() {
	fun printMessage(printer: (String)
}
原因：博客搬迁造成的旧链接失效。请回到主页。(*´﹃｀*)
